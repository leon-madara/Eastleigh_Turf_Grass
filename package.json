{
    "name": "turf-grass-project",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "lint": "eslint . --ext .js",
        "format": "prettier -w .",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "eslint": "^8.44.0",
        "gh-pages": "^6.3.0",
        "prettier": "^3.6.2",
        "vite": "^4.4.0"
    }
}
